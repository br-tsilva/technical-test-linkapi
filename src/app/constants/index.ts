import { database } from './database.constant'
import { pipedrive } from './pipedrive.constant'
import { bling } from './bling.constant'
import { server } from './server.constant'

export const constants = {
  database,
  pipedrive,
  bling,
  server,
}
